# Backend API URL used by the client-side code
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Backend API URL used by the server-side proxy
BACKEND_URL=http://localhost:4000

# Cookie name used for JWT auth tokens
AUTH_COOKIE_NAME=access_token
